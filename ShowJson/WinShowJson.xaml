<Window x:Class="ShowJson.WinShowJson"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:JsonGraphVisualizer.Views;assembly=JsonGraphVisualizer"
        Title="JSON Visualizer"
        Width="1400"
        Height="800"
        WindowStartupLocation="CenterScreen">


    <Window.Resources>
        <!-- 🎨 رنگ‌های داینامیک -->
        <SolidColorBrush x:Key="ScrollBarTrackBrush" Color="#3A3A3A"/>
        <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="#686868"/>
        <SolidColorBrush x:Key="ScrollBarThumbHoverBrush" Color="#9E9E9E"/>

        <!-- 🎨 Thumb Style -->
        <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="thumbBorder"
                                Background="{DynamicResource ScrollBarThumbBrush}"
                                CornerRadius="3"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="thumbBorder" 
                                        Property="Background" 
                                        Value="{DynamicResource ScrollBarThumbHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 🎨 Vertical ScrollBar -->
        <Style TargetType="ScrollBar">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Width" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{DynamicResource ScrollBarTrackBrush}">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbStyle}" Margin="2"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="10"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid Background="{DynamicResource ScrollBarTrackBrush}">
                                    <Track x:Name="PART_Track" IsDirectionReversed="False">
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumbStyle}" Margin="2"/>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid x:Name="MainGrid">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="325" MinWidth="150"/>
            <ColumnDefinition Width="2"/>
            <ColumnDefinition Width="*" MinWidth="200"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <TextBox x:Name="txtJsonInput"
                     
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="13"
                     MaxLength="327670"
                     Padding="5"/>

            <StackPanel Orientation="Vertical"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Margin="0,0,15,10">

                <Grid x:Name="chbGrid" Background="#e6e6e6">
                    <CheckBox x:Name="chbDarkMode"
                          Content="Dark Mode"
                          IsChecked="False"
                          Margin="0,0,0,8"
                          Checked="ChbDarkMode_CheckedChanged"
                          Unchecked="ChbDarkMode_CheckedChanged"/>
                </Grid>

                <Button x:Name="btnLoadJson"
                        Content="Load JSON"
                        Width="90"
                        Height="28"
                        Click="BtnLoadJson_Click"/>
            </StackPanel>
        </Grid>

        <GridSplitter x:Name="gridSplitter"
                      Grid.Column="1"
                      Width="2"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="#404040"
                      Cursor="SizeWE"/>

        <local:JsonGraphVisualizerControl x:Name="graphControl"
                                          Grid.Column="2"/>
    </Grid>
</Window>
