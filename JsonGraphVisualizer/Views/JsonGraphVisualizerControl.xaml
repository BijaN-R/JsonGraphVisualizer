<!-- Views/JsonGraphVisualizerControl.xaml -->
<UserControl x:Class="JsonGraphVisualizer.Views.JsonGraphVisualizerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:JsonGraphVisualizer.Converters"
             x:Name="RootControl"
             Background="{Binding ComponentBackground, ElementName=RootControl}"
             Focusable="True"
             FocusVisualStyle="{x:Null}"
             PreviewKeyDown="UserControl_PreviewKeyDown">

    <UserControl.Resources>
        <converters:BezierPathConverter x:Key="BezierPathConverter"/>
        <converters:ArrowPathConverter x:Key="ArrowPathConverter"/>
        <converters:TruncateTextConverter x:Key="TruncateTextConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>

        <!-- استایل برای Button های expand/collapse -->
        <Style x:Key="ExpandButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CompactContextMenu" TargetType="ContextMenu">
            <Setter Property="Background" Value="#181818"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HasDropShadow" Value="True"/>
        </Style>

        <!-- ✨ Style برای MenuItem -->
        <Style x:Key="CompactMenuItem" TargetType="MenuItem">
            <Setter Property="Background" Value="#181818"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,6"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderThickness="0"
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3E3E42"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CompactSeparator" TargetType="Separator">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Height="1" 
                        Background="{TemplateBinding Background}"
                        SnapsToDevicePixels="True"
                        Margin="0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Border BorderBrush="#3E3E42" BorderThickness="1">
            <ScrollViewer x:Name="MainScrollViewer"
                         HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Auto"
                         CanContentScroll="False"
                         PreviewMouseWheel="MainScrollViewer_PreviewMouseWheel"
                         PreviewMouseLeftButtonDown="MainScrollViewer_PreviewMouseLeftButtonDown"
                         PreviewMouseLeftButtonUp="MainScrollViewer_PreviewMouseLeftButtonUp"
                         PreviewMouseMove="MainScrollViewer_PreviewMouseMove">
                <Grid x:Name="ZoomContainer">
                    <Grid.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="ScaleTransform"/>
                            <TranslateTransform x:Name="TranslateTransform"/>
                        </TransformGroup>
                    </Grid.RenderTransform>

                    <Canvas x:Name="MainCanvas"
                        Background="Transparent"
                        Width="Auto"
                        Height="Auto"
                        RenderTransformOrigin="0.5,0.5">


                        <!-- لایه Edge ها (پشت Node ها) -->
                        <ItemsControl ItemsSource="{Binding Edges}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Canvas Visibility="{Binding Visibility}">
                                        <!-- خط منحنی -->
                                        <Path Stroke="{Binding EdgeBrush, ElementName=RootControl}"
                                          StrokeThickness="2">
                                            <Path.Data>
                                                <MultiBinding Converter="{StaticResource BezierPathConverter}">
                                                    <Binding Path="StartPoint"/>
                                                    <Binding Path="EndPoint"/>
                                                </MultiBinding>
                                            </Path.Data>
                                        </Path>

                                        <!-- فلش -->
                                        <Path Fill="{Binding EdgeBrush, ElementName=RootControl}"
                                          Data="{Binding EndPoint, Converter={StaticResource ArrowPathConverter}}"/>
                                    </Canvas>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- لایه Node ها -->
                        <ItemsControl ItemsSource="{Binding Nodes}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                                    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                                </Style>
                            </ItemsControl.ItemContainerStyle>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding NodeBackground, ElementName=RootControl}"
                                        BorderBrush="{Binding NodeBorderBrush, ElementName=RootControl}"
                                        BorderThickness="2"
                                        CornerRadius="5"
                                        Width="{Binding Width}"
                                        MinHeight="{Binding Height}"
                                        Visibility="{Binding Visibility}"
                                        Tag="Node"
                                        Cursor="Hand"
                                        MouseLeftButtonDown="Node_MouseLeftButtonDown">

                                        <Border.Effect>
                                            <DropShadowEffect Color="Black" 
                                                         Direction="315" 
                                                         ShadowDepth="3" 
                                                         BlurRadius="8" 
                                                         Opacity="0.5"/>
                                        </Border.Effect>

                                        <Border.ContextMenu>
                                            <ContextMenu Style="{StaticResource CompactContextMenu}">
                                                <MenuItem Header="🏠 Set as Root" 
                                                  Click="SetAsRoot_Click"
                                                  Style="{StaticResource CompactMenuItem}"/>
                                            </ContextMenu>
                                        </Border.ContextMenu>

                                        <StackPanel Margin="10">
                                            <!-- Header -->
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Expand/Collapse Button -->
                                                <Button Grid.Column="0"
                                                   Content="{Binding ExpandCollapseIcon}"
                                                   Style="{StaticResource ExpandButtonStyle}"
                                                   Visibility="{Binding HasChildren, Converter={StaticResource BoolToVisConverter}}"
                                                   Click="ExpandCollapseButton_Click"
                                                   Margin="0,0,5,0"/>

                                                <!-- Title -->
                                                <TextBlock Grid.Column="1"
                                                       Text="{Binding Title}"
                                                       FontFamily="{Binding GlobalFontFamily, ElementName=RootControl}"
                                                       FontSize="14"
                                                       FontWeight="Bold"
                                                       TextWrapping="Wrap"
                                                       VerticalAlignment="Center"
                                                       Cursor="Hand">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Type}" Value="Object">
                                                                    <Setter Property="Foreground" Value="{Binding ObjectTitleBrush, ElementName=RootControl}"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Type}" Value="Array">
                                                                    <Setter Property="Foreground" Value="{Binding ArrayTitleBrush, ElementName=RootControl}"/>
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Type}" Value="PropertyGroup">
                                                                    <Setter Property="Foreground" Value="{Binding ObjectTitleBrush, ElementName=RootControl}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>

                                            <!-- Separator -->
                                            <Border Height="1"
                                               Background="#3E3E42"
                                               Margin="0,0,0,8"/>

                                            <!-- Properties -->
                                            <ItemsControl ItemsSource="{Binding Properties}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Margin="0,2">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>

                                                            <TextBlock Grid.Column="0"
                                                                  Text="{Binding Key, StringFormat='{}{0}:'}"
                                                                  Foreground="{Binding KeyTextBrush, ElementName=RootControl}"
                                                                  FontFamily="{Binding GlobalFontFamily, ElementName=RootControl}"
                                                                  FontSize="11"
                                                                  Margin="0,0,5,0"/>

                                                            <TextBlock Grid.Column="1"
                                                                   Foreground="{Binding ValueTextBrush, ElementName=RootControl}"
                                                                   FontFamily="{Binding GlobalFontFamily, ElementName=RootControl}"
                                                                   FontSize="11"
                                                                   TextWrapping="Wrap">
                                                                <TextBlock.Text>
                                                                    <Binding Path="Value" Converter="{StaticResource TruncateTextConverter}"/>
                                                                </TextBlock.Text>
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Style.Triggers>
                                                                            <!-- اگر متن truncate شده، دکمه ... نمایش بده -->
                                                                            <DataTrigger Binding="{Binding Value, Converter={StaticResource TruncateTextConverter}}" Value="{x:Null}">
                                                                                <Setter Property="Cursor" Value="Arrow"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            
                                                            <!-- Hyperlink برای متن های truncate شده -->
                                                                <TextBlock.InputBindings>
                                                                    <MouseBinding MouseAction="LeftClick" 
                                                                            Command="{Binding ShowFullTextCommand}"/>
                                                                </TextBlock.InputBindings>
                                                                <TextBlock.ContextMenu>
                                                                    <ContextMenu Style="{StaticResource CompactContextMenu}">
                                                                        <MenuItem Header="📋 Copy Value" 
                                                                              Click="Value_Click"
                                                                              Style="{StaticResource CompactMenuItem}"/>
                                                                        <Separator Style="{StaticResource CompactSeparator}"/>
                                                                        <MenuItem Header="🏠 Set as Root" 
                                                                              Click="SetAsRoot_Click"
                                                                              Style="{StaticResource CompactMenuItem}"/>
                                                                    </ContextMenu>
                                                                </TextBlock.ContextMenu>
                                                            </TextBlock>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Canvas>
                </Grid>
            </ScrollViewer>
        </Border>
        <Button x:Name="ResetViewButton"
        Content="⌂" 
        VerticalContentAlignment="Top"
        HorizontalContentAlignment="Center"
        Width="40" Height="35"
        FontSize="20"
        Background="#06f"
        Foreground="White"
        BorderBrush="#222"
        Padding="0"
        HorizontalAlignment="Left"
        VerticalAlignment="Top"
        Margin="20"
        Click="ResetViewButton_Click"
        Opacity="0.65"
        Cursor="Hand">
        </Button>
        <Border x:Name="ToastHost"
            Background="#333"
            CornerRadius="6"
            Padding="10"
            Opacity="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="0,20,0,0"
            Panel.ZIndex="9999"
            Visibility="Collapsed">
            <TextBlock x:Name="ToastText"
                   FontSize="14"
                   TextWrapping="Wrap"/>
        </Border>
        <!-- 🔍 نوار جستجو -->
        <Border x:Name="SearchBar"
        Grid.Row="0"
        HorizontalAlignment="Right"
        VerticalAlignment="Top"
        Margin="20,15,20,0"
        Background="#2D2D30"
        BorderBrush="#1E8Ee2"
        BorderThickness="2"
        CornerRadius="4"
        Padding="10,8"
        Visibility="{Binding IsSearchVisible, ElementName=RootControl, Converter={StaticResource BoolToVisConverter}}">


            <StackPanel Orientation="Horizontal">
                <!-- Search Icon -->
                <TextBlock Text="🔍"
                   FontSize="14"
                   Foreground="LightGray"
                   Margin="0,0,8,0"
                   VerticalAlignment="Center"/>

                <!-- Search TextBox -->
                <TextBox x:Name="SearchTextBox"
                 Width="200"
                 CaretBrush="LightGray"
                 Background="Transparent"
                 Foreground="White"
                 BorderThickness="0"
                 FontSize="12"
                 VerticalAlignment="Center"
                 TextChanged="SearchTextBox_TextChanged"/>

                <!-- Match Counter -->
                <TextBlock x:Name="MatchCounterText"
                   Foreground="#858585"
                   FontSize="11"
                   Margin="10,0,10,0"
                   VerticalAlignment="Center"/>

                <!-- Next Button -->
                <Button x:Name="NextMatchButton"
                Content="▼"
                Width="24"
                Height="24"
                Background="Transparent"
                Foreground="White"
                BorderThickness="0"
                FontSize="10"
                Margin="2,0"
                Click="NextMatch_Click"
                ToolTip="Next Match (Enter)"/>

                <!-- Clear Button -->
                <Button x:Name="ClearSearchButton"
                Content="✕"
                Width="24"
                Height="24"
                Background="Transparent"
                Foreground="#858585"
                BorderThickness="0"
                FontSize="14"
                Margin="2,0,0,0"
                Click="ClearSearch_Click"
                ToolTip="Clear Search (Esc)"/>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
